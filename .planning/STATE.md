# State: AIOStreams-JF

## Project Reference
See: .planning/PROJECT.md (updated 2026-09-07)
**Core value:** Each AIOStreams profile is a complete Jellyfin server for its owner.
**Current focus:** Phase 3: Profile & Clients / Phase 4: Cut-over

## Position
Phase 1 COMPLETE 2026-09-08 (commits 419aa27b..f6b355d8, all pushed). Layer mounted at `/jellyfin`; login = profile UUID + password; env `ENABLE_JELLYFIN_API`; migrations 0027/0028; deps ws/@types/ws. Branching model (2026-09-08): `release` = upstream release tag + Jellyfin layer, the DEFAULT branch and the build source; `main` mirrors upstream dev, untouched. Upstream update: `git fetch upstream --tags && git checkout release && git merge vX.Y.Z` (conflicts only in hook files), build, tag `jf-vX.Y.Z`. Next: Phase 2 — build image, push to GHCR (tweakapps), parallel container on Dubai VPS with DB copy + own hostname.

## Session log
- 2026-09-07: project initialised after AioFin was discontinued.
- 2026-09-08: Phase 1 port complete; Fable reviewed hook diff (additive only).
- 2026-09-08: Phase 2 complete. Parallel container `aiostreams-jf` (image aiostreams-jf:2.34.0-jf1, built on the Dubai VPS) live at https://jf.tweakstreams.stream (project dir /home/ubuntu/aiostreams-jf, DB copy in ./data, env in ./.env chmod 600, README = rollback/cut-over). Jellyfin server name "TweakStreams DXB JF", reports Jellyfin 10.10.7. Production untouched. GHCR push deferred (token lacks write:packages).
- 2026-09-08 06:10: Phase 2 follow-ups — Authelia OIDC client `aiostreams-dxb` gained the jf callback (config backup taken, container restarted); image rebuilt with `scripts/generateMetadata.cjs` so /api/v1/status reports 2.34.0/v2.34.0/stable; VPS README has the rebuild recipe. Infuse authenticated from iPhone/Mac/Apple TV via the layer (log evidence). Phase 3 client checklist in progress by Maged.
- 2026-09-08 ~12:00 Dubai: Phase 3 client results — Infuse: FULL PASS (login via UUID/password, libraries from AioMetadata catalogs, seasons, playback, explicit picks, resume). SenPlayer: plays but shows no version picker → root cause: item-detail handler attaches real MediaSources only when `Fields=MediaSources` is requested (library.ts ~600-650); fix in progress: attach on single-item detail regardless, behind `JELLYFIN_ALWAYS_ATTACH_SOURCES` (default true), rebuild as 2.34.0-jf2. Also today: Authelia OIDC callback added for jf host; version metadata fixed. Trellis (Strand dev's standalone JF server, closed source, ghcr.io/lpierpoint/trellis) evaluated: not adopted — second config surface; kept as fallback only.
- 2026-09-08 12:40 Dubai: SenPlayer fix live (jf2, commit 82f94b05, `JELLYFIN_ALWAYS_ATTACH_SOURCES`). README banner + repo description added. Trackers research: AioMetadata (cedya77/aiometadata, branch dev) has NO progress endpoint; it check-ins to Trakt/Simkl/PublicMetaDB/MDBList/AniList/MAL from the Stremio SUBTITLES request (addon/lib/subtitleHandler.ts ~170-230). Risk: the JF layer resolves subtitles when building media sources → check-ins may fire on item OPEN, not play — Maged to verify in Trakt/Simkl history. NEXT PHASE (after cut-over): implement scrobbling in the layer from Jellyfin progress/stopped events (Trakt scrobble start/pause/stop, Simkl scrobble, PMDB check-in at completion threshold; tokens from the AIOStreams profile), and disable/suppress AioMetadata's subtitle check-in for the profile to avoid double/false entries. Inbound (Continue Watching/Up Next) stays via AioMetadata catalogs.
- 2026-09-08 ~17:00 Dubai: Fable code audit of the Jellyfin layer (report: /Users/magededward/claude-cc/aiostreams-jf-audit-2026-09-08.md). Root cause of Infuse tvOS "An error occurred": home-screen fan-out (48 catalog requests/s for 24 libraries) trips the borrowed stremio-catalog limiter (30/5s/IP) → 429s. Cast row empty: `peopleFrom` drops AioMetadata `app_extras.cast` photos/roles; person images 404. Phase 5 (Jellyfin Hardening) added with 4 Fable-authored plans; Sonnet executes; VPS-only commit 0f27caca merged into release as 672b2a72.
- 2026-09-08 ~18:00 Dubai: Phase 5 COMPLETE. Commits ad0a44cc (limiter+isolation), da912818 (service cache), 8bd18dec (cast photos/relay/stub), 1b83a752 (contract fixes). Image aiostreams-jf:2.34.0-jf3 (e970f71eca72) live at jf.tweakstreams.stream; rollback = retag jf2. Verified: 60 burst → 60×401/0×429; 429 body Jellyfin-shaped; production untouched. Maged to confirm in Infuse tvOS: home screen loads without error, cast row shows photos+roles. Known: core has a pre-existing import cycle (logger↔config↔tasks) that bites tests importing db-backed modules; dto.test.ts imports the core barrel first as a workaround.
- 2026-09-09: quick task — jf6 deployed (dedicated Jellyfin image rate limiter, 1500/5s device-keyed) after f22e90a6; verified 250/250 image requests 200, 0 static-rate-limit lines.
- 2026-09-09: Phase 6 COMPLETE (06-01 commit 52e11815: cheap detail attach-on-request/SenPlayer + single `jellyfin` device-keyed limiter replacing the split browse/image limiters; 06-02 commit 73da3d23: OfficialRating from app_extras.certification, Tmdb/Tvdb ProviderIds, director/writer photos, dedupe by name|type so a person can be both Actor and Director, episodes carry the series People, seasons use app_extras.seasonPosters, person pages recall the remembered photo + Etag; dto v3). Deployed aiostreams-jf:2.34.0-jf7 (59f85c104f7c) on the parallel container. Verified live: item detail (Mutiny) OfficialRating R, ProviderIds {Tmdb,Tvdb,Imdb}, Director with PrimaryImageTag, 30ms/4.5KB, zero subtitles-resource log lines; episode People.length=6; /Persons/Jason%20Statham has ImageTags.Primary; 300/300 image requests 200 zero 429; System/Info/Public 200. Production `aiostreams` untouched. Requirements DET-01, DET-02, META-01, META-02 all complete. Next: Phase 3 (Profile & Clients checklist, in progress by Maged) or Phase 4 (Cut-over) once Phase 3 evidence is in.
- 2026-09-09: Phase 7 COMPLETE (07-01 commit 36d67bd2: person filmography id decode/dedupe, similar strip case-insensitive genre match + search fallback + try/catch, search-only/empty-catalog hiding via `isKnownEmpty()` 10-min TTL, sort-once, person image cache-write memo; 07-02 commit 143ecb60: public-host artwork (TMDB/TVDB/metahub/Amazon/Fanart.tv) served by 302 with TMDB size rewrite to the smallest bucket >= requested width, `jellyfinMaxPlaybackSources` config caps PlaybackInfo at 20 by default (50 for `jellyfinAttachSourcesClients`) and drops MediaStreams beyond the first 5 sources, core test setup now imports the barrel once via `packages/core/test/setup.ts`, README security notes added). First deploy via the new GHCR-only pipeline (no VPS build): `ghcr.io/tweakapps/aiostreams-jf:release` @ `sha256:2ec9f966bedea33f249a2f6f6cb4a5c7b8cee9a2c1a61bc36110c625afe8e022` live on the parallel container (jf.tweakstreams.stream), production `aiostreams` untouched (uptime unaffected). Verified live: backdrop maxWidth=780 → 302 to `image.tmdb.org/t/p/w780/` in 43ms (cached; first request 2.2s while the image cache rebuilt); person Primary → 302 to a TMDB w185 URL; a non-TMDB-sourced Primary image (private addon proxy, webp) correctly stayed on the relay path (not a bug — that item's poster isn't TMDB-backed for this profile); PlaybackInfo Infuse-Direct = 20 sources, SenPlayer = 50; PersonIds query 72 items, Similar 12 items; UserViews hid `Voice Actor Roles` only after its first (empty) fetch populated the 10-min empty-catalog cache (expected TTL-warming behavior, not a regression) — Movies/Series/People Search stayed absent from a fresh restart; 77/77 parallel fan-out requests (UserViews + per-view Items + per-view Latest) returned 200; container log showed only pre-existing anime-DB-refresh 503 warnings unrelated to Jellyfin. Requirements FIX-01, FIX-02, FIX-03 all complete. Next: Phase 3 (Profile & Clients checklist, in progress by Maged) or Phase 4 (Cut-over) once Phase 3 evidence is in.
