# Quick task — content-based item Etag (client cache invalidation) → jf4

## Problem
`buildMetaItem` sets `Etag: imageTag(id)` (`packages/core/src/jellyfin/dto.ts:531`) and view items `Etag: imageTag(type:id)` (`:414`). The value never changes, so Jellyfin clients (Infuse keeps a local metadata store keyed on `Etag`) never refresh an item they have already seen. After jf3 added cast photos/roles, Infuse still shows the old cast for every item opened before the deploy (verified: server returns 18 People with `PrimaryImageTag`, images 200; Infuse shows none).

## Change
1. `dto.ts`: add `export const JELLYFIN_DTO_VERSION = 2;` near the top. Compute the item Etag from content:
   `Etag: imageTag(JSON.stringify([JELLYFIN_DTO_VERSION, id, meta.name, images.Primary, images.Backdrop, images.Logo, people.map(p => [p.Id, p.PrimaryImageTag, p.Role]), full.videos?.length ?? 0]))` — build `people` once before the item literal and reuse it for `People`. Also add `DateLastSaved` = `premiere ?? '2020-01-01T00:00:00.0000000Z'` is NOT acceptable (constant); leave `DateLastSaved` absent.
2. Same treatment for `buildEpisodeItem` and `buildSeasonItem`: add `Etag: imageTag(JSON.stringify([JELLYFIN_DTO_VERSION, id, title/name, images.Primary, overview length]))`. Do not add Etag to views beyond bumping to include `JELLYFIN_DTO_VERSION`.
3. Comment on the constant: "bump whenever the shape/content of items changes so clients drop cached metadata".
4. `pnpm build` zero errors; `pnpm --filter ./packages/core test` green (53). Commit `fix(jellyfin): content-based item Etag so clients refresh cached metadata (dto v2)`; push `origin release`.
5. Deploy exactly as plan 05-04 Part B but tag `aiostreams-jf:2.34.0-jf4`: on the VPS `git -C /home/ubuntu/aiostreams-jf/src fetch origin && git -C /home/ubuntu/aiostreams-jf/src reset --hard origin/release` (tree must be clean first), `docker build -t aiostreams-jf:2.34.0-jf4 /home/ubuntu/aiostreams-jf/src`, compose tag jf3→jf4, `docker compose up -d`, verify System/Info/Public 200 and that `/jellyfin/c05bd9bc-88a1-44c7-b674-6c1ea9da5a2d/<encrypted-password>/Items/a11101000001ed72edffffffff000000` (encrypted password read from `config_profiles` in `/home/ubuntu/aiostreams-jf/data/aiostreams/db.sqlite` with python3 sqlite3 — never print it) returns an `Etag` different from `742c20aa05c0f3b0`. Production `aiostreams` untouched. Update the VPS README image tag line. SUMMARY.md ≤15 lines here, commit, push.
